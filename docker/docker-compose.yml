version: '3.4'

services:
  develop:
    build:
      context: .
      dockerfile: Dockerfile
    stdin_open: true
    tty: true

# docker-compose ps
# docker-compose up -d
# docker-compose down
# docker-compose exec develop bash